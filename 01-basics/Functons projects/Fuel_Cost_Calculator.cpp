// Testing value-returning functions - fuel cost calculator

#include <iostream>

int getDistance(){
    int distance{};
    std::cout<<"Enter The Distance(KM): "

}
getMileage() — mileage lo (km/litre)
getFuelPrice() — fuel price lo (Rs/litre)
calculateFuelCost() — cost calculate karo (distance / mileage) * fuelPrice
main() 